doctype transitional
html
  head
    title Reset your Password | Willow
    meta(http-equiv="Content-Type" content="text/html; charset=UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
  body(style="margin: 0px; padding: 0px;")
    table(cellpadding="0" cellspacing="0" width="100%")
      tr
        td
          table(align="center" cellpadding="10" cellspacing="0" width="600" style="border: 1px solid #cccccc;")
            tr
              td(bgcolor="#bbddbb")
                table(cellpadding="0" cellspacing="0" width="100%")
                  tr
                    td(width="120")
                      img(src=`${baseUrl}/img/logo.png` width="100" height="116")
                    td
                      h1(style="color: #000000;")
                        font(color="#000000") Willow
            tr
              td
                table(cellpadding="0" cellspacing="30" width="100%")
                  tr
                    td(align="center" bgcolor="#ffffff") Use the link below to reset your password.
                  tr
                    td
                      table(align="center" cellpadding="10" cellspacing="0" width="200")
                        tr
                          td(align="center" bgcolor="#4444cc")
                            a(href=`${baseUrl}/auth/reset?token=${token}` style="color: #ffffff; text-decoration: none;")
                              font(color="#ffffff") Reset your password
            tr
              td(bgcolor="#eeeeee") If you did not request to reset your password, then you can safely ignore this email. This link will expire after 24 hours.
